# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# dynamic seed config (per-client, never commit)
seed.config.json

# typescript
*.tsbuildinfo
next-env.d.ts
node_modules
.vercel

# graphify (knowledge graph)
graphify-out/

# local seed config (per-database store configuration)
seed.config.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# database snapshots (raw data, never commit)
/db-backups/

# client releases (independent folders, never commit)
/releases/

# crash dumps and temporary cache
/.next.corrupto/
.devserver.*

# dev server / debug logs (never commit)
dev.log
dev.err
dev.out
devlog.*
output.txt
*.log
